{
  "name": "pathos-dacs-ref",
  "version": "0.1.0",
  "description": "PATH-OS reference implementation of DACS \u2014 Demos Agent Commerce Standards (paper v0.7, DACS-1..5 v0.1). Scope: DACS-1 listing publisher, DACS-5 envelope-receipt verifier CLI, DACS-2 evm-rpc + GLEIF consensus-backed-proxy recipe.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "repro:dacs-v01-golden-pay-binding": "tsx conformance/reproducers/dacs-v01-golden-pay-binding/reproduce.mts",
    "listing-pub": "tsx src/cli/listing-pub.ts",
    "discovery-gen": "tsx src/cli/discovery-gen.ts",
    "verify": "tsx src/cli/verify.ts",
    "vet-gleif": "tsx src/cli/vet-gleif.ts",
    "test": "tsx --test test/vectors/*.test.ts test/integration/*.test.ts",
    "convergence": "tsx conformance/convergence-suite.mts",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "@kynesyslabs/demosdk": "^4.0.10",
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.5.0",
    "canonicalize": "^2.0.0"
  },
  "_deps_note": "@noble/hashes is a direct dep (imported by src/demos/dahr.ts, src/demos/storage.ts, src/jcs.ts) AND a transitive dep of @noble/ed25519; declared here to make the direct import explicit.",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "patch-package": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
