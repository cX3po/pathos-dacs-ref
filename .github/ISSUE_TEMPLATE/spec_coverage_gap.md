---
name: Spec-coverage gap
about: A DACS section you'd like to see covered (or covered better)
title: '[coverage] '
labels: coverage
---

## Section requested

(Cite the spec § — e.g. "§7.3.5 OFAC SDN recipe", "§9.5.4 HTLC settlement",
"§10.5.1 reputation derivation".)

## Current state

(What does the implementation do today? Out of scope? Stubbed? Partial?
Cross-reference `docs/SPEC-COVERAGE.md` if it says.)

## What "covered" would look like

(Concrete: which CLI behavior changes, which new tests pass, which new code
path exists. If you have a spec-text snippet that defines the conformance bar,
quote it verbatim.)

## Dependencies

(Does this need DACS-3 or DACS-4 to land first? Does it require a substrate
capability that's not yet shipped (SR-3 wire protocol, SR-4 channel)?)

## Priority signal

(Optional: why is this section interesting to cover now vs. later? Is there a
real use case behind the ask?)
